[package]
name = "claude-hooks"
version = "0.2.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.10"
once_cell = "1.19"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"
